services:
  castle:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - castle-data:/app/data
    environment:
      - DATABASE_PATH=/app/data/castle.db
      - UPLOADS_PATH=/app/data/uploads
    restart: unless-stopped

volumes:
  castle-data:
